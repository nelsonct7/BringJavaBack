package Intro;
/*
 * ArrayList
 * 1. Array list is based on array operations
 * 2. Manipulation with array list is slow, bcz it internally uses array, and when an item is removed from ArrayList all elements are bit shifted
 * 3. Array list consumes less memory than linked list 
 * 4. Array list class can act as list operations only, since it implements 
 * 5. Array list is better for sorting and accessing data
 * LinkedLIst
 * 1. Linked list is implemented with three components Prev=> reference to previous node, Data=>Where data is stored, Next => reference to next node in linked list	
 * 2. Manipulation with linked list is faster bcz it uses doubly linked list internally, so no bitwise shift is required
 * 3. Linked list consumes more memory than array list.
 * 4. Linked list class can act as list and que since it implements deque and list
 * 5. Linked list is better for manipulating data.
 */
public class DifferenceBtwArrayListAndLinkedList {

}
